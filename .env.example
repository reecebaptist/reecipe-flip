# Copy this to .env.local for local development (CRA will read .env.local)
# These values are safe to expose to the browser. Do NOT put secrets here.
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=
